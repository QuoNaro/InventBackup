name: 🐳 Сборка и релиз Docker-образа

on:
  push:
    tags:
      - 'v*'  # Запускается при создании тега, начинающегося с 'v'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Получение кода
        uses: actions/checkout@v3

      - name: ⚙️ Настройка Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: test
        run: | 
          pwd 
          ls
          ls source

      - name: 🏗️ Сборка Docker-образа
        run: |
          docker build -t mssql-duplica-script:latest  -f ./source/Dockerfile ./source

      - name: 📦 Сохранение и архивирование Docker-образа
        run: |
          docker save mssql-duplica-script:latest -o mssql-duplica-script.tar
          zip -r mssql-duplica-script.zip  ./files-for-image/* ./mssql-duplica-script.tar

      - name: 🚀 Создание релиза
        uses: softprops/action-gh-release@v2.0.8
        with:
          tag_name: ${{ github.ref_name }}
          files: |
            mssql-duplica-script.zip
            ./files-for-image/README.md
        env:
          GITHUB_TOKEN: ${{ secrets.PAT }}