name: 🐳 Сборка и релиз Docker-образа

on:
  push:
    tags:
      - 'v*'  # Запускается при создании тега, начинающегося с 'v'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Получение кода
        uses: actions/checkout@v3

      - name: ⚙️ Настройка Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: 🏗️ Сборка Docker-образа
        id: build
        uses: docker/build-push-action@v2
        with:
          context: .
          file: Dockerfile
          push: false  # Не отправляем в Docker Hub
          tags: ms-duplica-script:${{ github.ref_name }}

      - name: 📦 Сохранение и архивирование Docker-образа
        run: |
          docker save ms-duplica-script:${{ github.ref_name }} -o ms-duplica-script.tar
          zip mssql-duplica-script.zip ms-duplica-script.tar

      - name: 🚀 Создание релиза
        uses: softprops/action-gh-release@v2.0.8
        with:
          tag_name: ${{ github.ref_name }}
          files: |
            mssql-duplica-script.zip  # Загружаем архивированный файл
            .image-files/README.md # Загружаем инструкцию для релиза
            .env.example
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}